<% @masjids.each do |m| %>
  <div class="masjid">
    <div><%= link_to m.name.titleize, masjid_path(m), class: "name" %></div>
    <div>address: <%= m.village  %> <%= m.city  %></div>
  </div>
<% end %>
